<!DOCTYPE html>
<html>
  <head>
    <title><%= director.name %></title>
  </head>
  <body>
    <main>
      <a class="btn ghost" href="/directors">← All Directors</a>
      <div style="height: 12px"></div>
      <div class="profile-show">
        <div>
          <img
            class="profile-image"
            alt="<%= director.name %>"
            src="<%= director.image_url || director.photo_url || ('/profiles/' + director.director_id + '.jpg') || '/profile-placeholder.svg' %>"
          />
        </div>
        <section class="detail-section">
          <h1 style="margin-top: 0"><%= director.name %></h1>
          <ul class="meta-list">
            <li>Born: <strong><%= director.birth_year || '—' %></strong></li>
            <li>
              Nationality: <strong><%= director.nationality || '—' %></strong>
            </li>
          </ul>

          <div style="margin-top: 14px">
            <a class="btn" href="/actors/<%= director.director_id %>/edit"
              >Edit</a
            >
            <form
              action="/actors/<%= director.director_id %>?_method=DELETE"
              method="POST"
              style="display: inline; margin-left: 8px"
            >
              <button class="btn ghost" type="submit">Delete</button>
            </form>
          </div>
        </section>
      </div>
    </main>
  </body>
</html>
