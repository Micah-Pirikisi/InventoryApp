<!DOCTYPE html>
<html>
  <head>
    <title><%= actor.name %></title>
  </head>
  <body>
    <main>
      <a class="btn ghost" href="/actors">← All Actors</a>
      <div style="height: 12px"></div>
      <div class="profile-show">
        <div>
          <img
            class="profile-image"
            alt="<%= actor.name %>"
            src="<%= actor.image_url || actor.photo_url || ('/profiles/' + actor.actor_id + '.jpg') || '/profile-placeholder.svg' %>"
          />
        </div>
        <section class="detail-section">
          <h1 style="margin-top: 0"><%= actor.name %></h1>
          <ul class="meta-list">
            <li>Born: <strong><%= actor.birth_year || '—' %></strong></li>
            <li>
              Nationality: <strong><%= actor.nationality || '—' %></strong>
            </li>
          </ul>

          <div style="margin-top: 14px">
            <a class="btn" href="/actors/<%= actor.actor_id %>/edit">Edit</a>
            <form
              action="/actors/<%= actor.actor_id %>?_method=DELETE"
              method="POST"
              style="display: inline; margin-left: 8px"
            >
              <button class="btn ghost" type="submit">Delete</button>
            </form>
          </div>
        </section>
      </div>
    </main>
  </body>
</html>
