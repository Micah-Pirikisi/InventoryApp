<!DOCTYPE html>
<html>
  <head>
    <title><%= category.name %></title>
  </head>
  <body>
    <h1><%= category.name %></h1>
    <p><%= category.description %></p>

    <a class="btn" href="/categories/<%= category.category_id %>/edit">Edit</a>
    <form
      action="/categories/<%= category.category_id %>?_method=DELETE"
      method="POST"
      style="display: inline"
    >
      <button class="btn ghost" type="submit">Delete</button>
    </form>
    <br />
    <a class="btn ghost" href="/categories">Back to all categories</a>
  </body>
</html>
